create the parent project directory
create a README.MD
create an app directory - which will contain all the API source codes
create api, core,  directory inside app directory.
    api - controllers/routers, service, model, repository
    core - logger, security, config, database
    main.py - root file that starts the api
create a tests folder for writing test cases


goto route directory
create a virtual environment -- python -m venv interview


`main.py` -> FastAPI()
`user_router.py` -> APIRouter()
